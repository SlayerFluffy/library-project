services:
  - type: web
    name: library-project
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
      - key: MONGODB_URL
        sync: false
